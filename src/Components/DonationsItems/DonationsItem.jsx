const DonationsItem = ({ donations }) => {
  const {
    card_bg,
    category,
    category_bg,
    id,
    image,
    price,
    text_button_bg,
    title,
  } = donations;
  // const {}
  // console.log(donations);
  return (
    <>
     <h2>Hello</h2>
    </>
  );
};

export default DonationsItem;
